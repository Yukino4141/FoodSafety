<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.itheima.server.mapper.FamilyMemberMapper">

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO family_member (user_id, name, age, health_tags, create_time)
        VALUES (#{userId}, #{name}, #{age}, #{healthTags}, #{createTime})
    </insert>

    <select id="listByUserId" resultType="com.itheima.pojo.entity.FamilyMember">
        SELECT * FROM family_member WHERE user_id = #{userId} ORDER BY create_time DESC
    </select>

</mapper>
