<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.itheima.server.mapper.UserPointsMapper">

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user_points (user_id, points, task_type, remark, create_time)
        VALUES (#{userId}, #{points}, #{taskType}, #{remark}, #{createTime})
    </insert>

</mapper>
